import * as React from 'react'
import {AppTypes} from '../../../App'

interface UserActionsTypes extends AppTypes {}

const UserActions = (props: UserActionsTypes) => {
    return (
        <div id="user-actions-drawer">

        </div>
    )
}

export default UserActions