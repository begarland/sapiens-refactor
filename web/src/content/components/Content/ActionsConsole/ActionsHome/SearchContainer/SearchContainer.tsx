import * as React from 'react'

interface SearchContainerTypes {

}

const SearchContainer = (props: SearchContainerTypes) => {
    return (
        <div id="search-container">
            <h1>Search bar</h1>
        </div>
    )
}

export default SearchContainer